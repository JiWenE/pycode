互联网上所有软件的基本原理：
所有的网络程序，本质上都是cs架构
客户端--网线--网络传输协议--通过网站传输数据--服务端

服务端：一个程序（一台电脑）的程序

URL 同意资源定位符， 网址属于url
爬虫：
    spider 自动浏览网页信息
正则入门：
对字符串的逻辑公式，过滤字符串

. 可以匹配1个任意字符（不包括换行）
* 代表他前面的表达式匹配任意次数(贪婪匹配)
? 代表费贪婪匹配
() 代表子表达式，会吧匹配的内容放进换错，返回